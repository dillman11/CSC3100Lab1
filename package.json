{
  "name": "project-1",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=react-frontend\" \"npm run dev --workspace=express-backend\"",
    "build": "npm run build --workspace=react-frontend",
    "lint": "npm run lint --workspace=react-frontend",
    "preview": "npm run preview --workspace=react-frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^10.0.5",
    "nodemon": "^3.1.14"
  },
  "dependencies": {
    "cors": "^2.8.6"
  }
}
